# MediKiosk design system

The three applications deliberately share a compact clinical visual language: dark navy for secure clinical workspaces, teal for active care actions, warm warning tones for potential early-attention prompts, large touch targets in the kiosk, and visible keyboard focus indicators. App-level CSS is kept local to make each screen deployable independently.
